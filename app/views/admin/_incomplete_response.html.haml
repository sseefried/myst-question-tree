.incomplete= render :partial => 'show_response', :locals => { :response => response }
%span.tiny-text{:id => "response_#{response.id}"}
=render :partial => 'add_result', :locals => { :response => response }
%span.tiny-text
 %i or
=render :partial => 'link_to_question', :locals => { :response => response }
