%span{:id => result_css_id(response,result)}
  = result.id
  %span.tiny-text= link_to_remote "X", :url => { :action => 'delete_result', :id => response.id, :result_id => result.id }, :confirm => "Are you sure you want to delete this result? Deleting it will remove it from all responses it's currently related to!"
  %span.tiny-text= "|"
